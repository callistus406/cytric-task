/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.pexels.com", "localhost","via.placeholder.com","images.unsplash.com"],
  },
};

export default nextConfig;